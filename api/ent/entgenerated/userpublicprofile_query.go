// Code generated by ent, DO NOT EDIT.

package entgenerated

import (
	"api/ent/entgenerated/predicate"
	"api/ent/entgenerated/user"
	"api/ent/entgenerated/userpublicprofile"
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserPublicProfileQuery is the builder for querying UserPublicProfile entities.
type UserPublicProfileQuery struct {
	config
	ctx        *QueryContext
	order      []userpublicprofile.OrderOption
	inters     []Interceptor
	predicates []predicate.UserPublicProfile
	withOwner  *UserQuery
	modifiers  []func(*sql.Selector)
	loadTotal  []func(context.Context, []*UserPublicProfile) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserPublicProfileQuery builder.
func (uppq *UserPublicProfileQuery) Where(ps ...predicate.UserPublicProfile) *UserPublicProfileQuery {
	uppq.predicates = append(uppq.predicates, ps...)
	return uppq
}

// Limit the number of records to be returned by this query.
func (uppq *UserPublicProfileQuery) Limit(limit int) *UserPublicProfileQuery {
	uppq.ctx.Limit = &limit
	return uppq
}

// Offset to start from.
func (uppq *UserPublicProfileQuery) Offset(offset int) *UserPublicProfileQuery {
	uppq.ctx.Offset = &offset
	return uppq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uppq *UserPublicProfileQuery) Unique(unique bool) *UserPublicProfileQuery {
	uppq.ctx.Unique = &unique
	return uppq
}

// Order specifies how the records should be ordered.
func (uppq *UserPublicProfileQuery) Order(o ...userpublicprofile.OrderOption) *UserPublicProfileQuery {
	uppq.order = append(uppq.order, o...)
	return uppq
}

// QueryOwner chains the current query on the "owner" edge.
func (uppq *UserPublicProfileQuery) QueryOwner() *UserQuery {
	query := (&UserClient{config: uppq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := uppq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := uppq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userpublicprofile.Table, userpublicprofile.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, userpublicprofile.OwnerTable, userpublicprofile.OwnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(uppq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserPublicProfile entity from the query.
// Returns a *NotFoundError when no UserPublicProfile was found.
func (uppq *UserPublicProfileQuery) First(ctx context.Context) (*UserPublicProfile, error) {
	nodes, err := uppq.Limit(1).All(setContextOp(ctx, uppq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userpublicprofile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uppq *UserPublicProfileQuery) FirstX(ctx context.Context) *UserPublicProfile {
	node, err := uppq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserPublicProfile ID from the query.
// Returns a *NotFoundError when no UserPublicProfile ID was found.
func (uppq *UserPublicProfileQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uppq.Limit(1).IDs(setContextOp(ctx, uppq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userpublicprofile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uppq *UserPublicProfileQuery) FirstIDX(ctx context.Context) int {
	id, err := uppq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserPublicProfile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserPublicProfile entity is found.
// Returns a *NotFoundError when no UserPublicProfile entities are found.
func (uppq *UserPublicProfileQuery) Only(ctx context.Context) (*UserPublicProfile, error) {
	nodes, err := uppq.Limit(2).All(setContextOp(ctx, uppq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userpublicprofile.Label}
	default:
		return nil, &NotSingularError{userpublicprofile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uppq *UserPublicProfileQuery) OnlyX(ctx context.Context) *UserPublicProfile {
	node, err := uppq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserPublicProfile ID in the query.
// Returns a *NotSingularError when more than one UserPublicProfile ID is found.
// Returns a *NotFoundError when no entities are found.
func (uppq *UserPublicProfileQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uppq.Limit(2).IDs(setContextOp(ctx, uppq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userpublicprofile.Label}
	default:
		err = &NotSingularError{userpublicprofile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uppq *UserPublicProfileQuery) OnlyIDX(ctx context.Context) int {
	id, err := uppq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserPublicProfiles.
func (uppq *UserPublicProfileQuery) All(ctx context.Context) ([]*UserPublicProfile, error) {
	ctx = setContextOp(ctx, uppq.ctx, "All")
	if err := uppq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserPublicProfile, *UserPublicProfileQuery]()
	return withInterceptors[[]*UserPublicProfile](ctx, uppq, qr, uppq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uppq *UserPublicProfileQuery) AllX(ctx context.Context) []*UserPublicProfile {
	nodes, err := uppq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserPublicProfile IDs.
func (uppq *UserPublicProfileQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uppq.ctx.Unique == nil && uppq.path != nil {
		uppq.Unique(true)
	}
	ctx = setContextOp(ctx, uppq.ctx, "IDs")
	if err = uppq.Select(userpublicprofile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uppq *UserPublicProfileQuery) IDsX(ctx context.Context) []int {
	ids, err := uppq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uppq *UserPublicProfileQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uppq.ctx, "Count")
	if err := uppq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uppq, querierCount[*UserPublicProfileQuery](), uppq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uppq *UserPublicProfileQuery) CountX(ctx context.Context) int {
	count, err := uppq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uppq *UserPublicProfileQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uppq.ctx, "Exist")
	switch _, err := uppq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("entgenerated: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uppq *UserPublicProfileQuery) ExistX(ctx context.Context) bool {
	exist, err := uppq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserPublicProfileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uppq *UserPublicProfileQuery) Clone() *UserPublicProfileQuery {
	if uppq == nil {
		return nil
	}
	return &UserPublicProfileQuery{
		config:     uppq.config,
		ctx:        uppq.ctx.Clone(),
		order:      append([]userpublicprofile.OrderOption{}, uppq.order...),
		inters:     append([]Interceptor{}, uppq.inters...),
		predicates: append([]predicate.UserPublicProfile{}, uppq.predicates...),
		withOwner:  uppq.withOwner.Clone(),
		// clone intermediate query.
		sql:  uppq.sql.Clone(),
		path: uppq.path,
	}
}

// WithOwner tells the query-builder to eager-load the nodes that are connected to
// the "owner" edge. The optional arguments are used to configure the query builder of the edge.
func (uppq *UserPublicProfileQuery) WithOwner(opts ...func(*UserQuery)) *UserPublicProfileQuery {
	query := (&UserClient{config: uppq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	uppq.withOwner = query
	return uppq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OwnerID int `json:"owner_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserPublicProfile.Query().
//		GroupBy(userpublicprofile.FieldOwnerID).
//		Aggregate(entgenerated.Count()).
//		Scan(ctx, &v)
func (uppq *UserPublicProfileQuery) GroupBy(field string, fields ...string) *UserPublicProfileGroupBy {
	uppq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserPublicProfileGroupBy{build: uppq}
	grbuild.flds = &uppq.ctx.Fields
	grbuild.label = userpublicprofile.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OwnerID int `json:"owner_id,omitempty"`
//	}
//
//	client.UserPublicProfile.Query().
//		Select(userpublicprofile.FieldOwnerID).
//		Scan(ctx, &v)
func (uppq *UserPublicProfileQuery) Select(fields ...string) *UserPublicProfileSelect {
	uppq.ctx.Fields = append(uppq.ctx.Fields, fields...)
	sbuild := &UserPublicProfileSelect{UserPublicProfileQuery: uppq}
	sbuild.label = userpublicprofile.Label
	sbuild.flds, sbuild.scan = &uppq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserPublicProfileSelect configured with the given aggregations.
func (uppq *UserPublicProfileQuery) Aggregate(fns ...AggregateFunc) *UserPublicProfileSelect {
	return uppq.Select().Aggregate(fns...)
}

func (uppq *UserPublicProfileQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uppq.inters {
		if inter == nil {
			return fmt.Errorf("entgenerated: uninitialized interceptor (forgotten import entgenerated/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uppq); err != nil {
				return err
			}
		}
	}
	for _, f := range uppq.ctx.Fields {
		if !userpublicprofile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("entgenerated: invalid field %q for query", f)}
		}
	}
	if uppq.path != nil {
		prev, err := uppq.path(ctx)
		if err != nil {
			return err
		}
		uppq.sql = prev
	}
	if userpublicprofile.Policy == nil {
		return errors.New("entgenerated: uninitialized userpublicprofile.Policy (forgotten import entgenerated/runtime?)")
	}
	if err := userpublicprofile.Policy.EvalQuery(ctx, uppq); err != nil {
		return err
	}
	return nil
}

func (uppq *UserPublicProfileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserPublicProfile, error) {
	var (
		nodes       = []*UserPublicProfile{}
		_spec       = uppq.querySpec()
		loadedTypes = [1]bool{
			uppq.withOwner != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserPublicProfile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserPublicProfile{config: uppq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(uppq.modifiers) > 0 {
		_spec.Modifiers = uppq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uppq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := uppq.withOwner; query != nil {
		if err := uppq.loadOwner(ctx, query, nodes, nil,
			func(n *UserPublicProfile, e *User) { n.Edges.Owner = e }); err != nil {
			return nil, err
		}
	}
	for i := range uppq.loadTotal {
		if err := uppq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (uppq *UserPublicProfileQuery) loadOwner(ctx context.Context, query *UserQuery, nodes []*UserPublicProfile, init func(*UserPublicProfile), assign func(*UserPublicProfile, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserPublicProfile)
	for i := range nodes {
		fk := nodes[i].OwnerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "owner_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (uppq *UserPublicProfileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uppq.querySpec()
	if len(uppq.modifiers) > 0 {
		_spec.Modifiers = uppq.modifiers
	}
	_spec.Node.Columns = uppq.ctx.Fields
	if len(uppq.ctx.Fields) > 0 {
		_spec.Unique = uppq.ctx.Unique != nil && *uppq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uppq.driver, _spec)
}

func (uppq *UserPublicProfileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userpublicprofile.Table, userpublicprofile.Columns, sqlgraph.NewFieldSpec(userpublicprofile.FieldID, field.TypeInt))
	_spec.From = uppq.sql
	if unique := uppq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uppq.path != nil {
		_spec.Unique = true
	}
	if fields := uppq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userpublicprofile.FieldID)
		for i := range fields {
			if fields[i] != userpublicprofile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if uppq.withOwner != nil {
			_spec.Node.AddColumnOnce(userpublicprofile.FieldOwnerID)
		}
	}
	if ps := uppq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uppq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uppq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uppq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uppq *UserPublicProfileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uppq.driver.Dialect())
	t1 := builder.Table(userpublicprofile.Table)
	columns := uppq.ctx.Fields
	if len(columns) == 0 {
		columns = userpublicprofile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uppq.sql != nil {
		selector = uppq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uppq.ctx.Unique != nil && *uppq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uppq.predicates {
		p(selector)
	}
	for _, p := range uppq.order {
		p(selector)
	}
	if offset := uppq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uppq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserPublicProfileGroupBy is the group-by builder for UserPublicProfile entities.
type UserPublicProfileGroupBy struct {
	selector
	build *UserPublicProfileQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (uppgb *UserPublicProfileGroupBy) Aggregate(fns ...AggregateFunc) *UserPublicProfileGroupBy {
	uppgb.fns = append(uppgb.fns, fns...)
	return uppgb
}

// Scan applies the selector query and scans the result into the given value.
func (uppgb *UserPublicProfileGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, uppgb.build.ctx, "GroupBy")
	if err := uppgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserPublicProfileQuery, *UserPublicProfileGroupBy](ctx, uppgb.build, uppgb, uppgb.build.inters, v)
}

func (uppgb *UserPublicProfileGroupBy) sqlScan(ctx context.Context, root *UserPublicProfileQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(uppgb.fns))
	for _, fn := range uppgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*uppgb.flds)+len(uppgb.fns))
		for _, f := range *uppgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*uppgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := uppgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserPublicProfileSelect is the builder for selecting fields of UserPublicProfile entities.
type UserPublicProfileSelect struct {
	*UserPublicProfileQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (upps *UserPublicProfileSelect) Aggregate(fns ...AggregateFunc) *UserPublicProfileSelect {
	upps.fns = append(upps.fns, fns...)
	return upps
}

// Scan applies the selector query and scans the result into the given value.
func (upps *UserPublicProfileSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, upps.ctx, "Select")
	if err := upps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserPublicProfileQuery, *UserPublicProfileSelect](ctx, upps.UserPublicProfileQuery, upps, upps.inters, v)
}

func (upps *UserPublicProfileSelect) sqlScan(ctx context.Context, root *UserPublicProfileQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(upps.fns))
	for _, fn := range upps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*upps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := upps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
